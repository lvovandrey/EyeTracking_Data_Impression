<UserControl x:Class="DataImpression.View.StatisticsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataImpression.View"
             xmlns:elements="clr-namespace:DataImpression.View.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="This">
    <Grid>
        <StackPanel Orientation="Vertical">
            <elements:PropertyViewer Tag="{Binding FullTimeName}" DataContext="{Binding FullTime}"></elements:PropertyViewer>
            <elements:PropertyViewer Tag="{Binding FixationsFullCountName}" DataContext="{Binding FixationsFullCount}"></elements:PropertyViewer>
            <elements:PropertyViewer Tag="{Binding FrequencyRequestsToAnyFAOIPerMinuteName}" DataContext="{Binding FrequencyRequestsToAnyFAOIPerMinute}"></elements:PropertyViewer>
        </StackPanel> 
    </Grid>
</UserControl>
