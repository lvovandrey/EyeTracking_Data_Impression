<UserControl x:Class="DataImpression.Tests.TestScrollableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:DataImpression.Tests"
         d:DesignHeight="250" d:DesignWidth="400"
        mc:Ignorable="d">
    <UserControl.DataContext>
        <local:TestScrollableViewModel></local:TestScrollableViewModel>
    </UserControl.DataContext>
    <Grid x:Name="Gridd">
        <lvc:CartesianChart Name="Chart" Width="{Binding ElementName}" Series="{Binding SeriesCollection}" LegendLocation="None" Grid.Row="1" Foreground="Green" MouseWheel="CartesianChart_MouseWheel" >
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Зона" Labels="{Binding Labels}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Процент времени фиксаций, %" LabelFormatter="{Binding Formatter}" MinValue="0"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
